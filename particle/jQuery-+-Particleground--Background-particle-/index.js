$(function($) {
	var settings = {
		// dotColor: "turquoise",
		// lineColor: "turquoise",
		dotColor: "white",
		lineColor: "white",
		particleRadius: 10
	};
	$("#particles").particleground(settings);
});